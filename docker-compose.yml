version: '3.1'
services:
  web:
    image: odoo:16.0
    container_name: odoo-16.0
    depends_on:
      - db
    ports:
      - "8016:8069"
    environment:
      - HOST=db
      - USER=odoo
      - PASSWORD=odoo
    volumes:
      - ./addons/extra-addons:/mnt/extra-addons
      - ./addons/enterprise:/mnt/enterprise
      - ./odoo.conf:/etc/odoo/odoo.conf
    command: -d test

  db:
    image: postgres:13
    container_name: postgres-13
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo